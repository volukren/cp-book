#include <bits/stdc++.h>

#define int long long
#define all(x) (x).begin(), (x).end()
#define sz(x) ((x).size())

using namespace std;

int32_t main() {
    cin.tie(nullptr); 
    ios::sync_with_stdio(false);
    
    return 0;
}
